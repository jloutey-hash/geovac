# Sprint Topos-2 — Family-1 externality as frame-meet collapse

**Date:** 2026-07-05 · **PI-directed** ("let's find out" — the Topos-1 follow-on) · **Verdict: GO** — the multi-focal (Family-1) externality mechanism is formalized at the site level on one corpus-faithful instance, with every load-bearing step exact.

## 1. The question

Topos-1 left one leg open: Family 1 (the 17 multi-focal calibration entries) — the proposal being that its externality is *frame-composition failure*, structurally different from Family 2's ab-initio KS obstruction (as Paper 57's two-family decomposition requires: same surface verdict, different structural reasons).

## 2. The instance and the two theorems

**Instance:** two same-center hydrogenic Fock frames at exponents Z = 1 vs Z′ ∈ {2, 3} — exactly the mixed-exponent setting of the Track-DF / Löwdin wall (§3 rows: heterogeneous nested → Löwdin destroys Gaunt sparsity). All computations use exact Fractions on unnormalized overlaps (positive normalizations cannot change support), so the load-bearing claims are bit-exact.

**Meet theorem (computed, 4-cell panel: Z′ ∈ {2,3} × n_max ∈ {3,4}).** The inter-frame overlap is exactly (l,m)-block-diagonal (rotational symmetry), and within every block the exact support graph is **connected**. Hence the meet of the two frame MASAs is **exactly the angular-grading algebra**: dim collapse 14 → 9 (n_max = 3) and 30 → 16 (n_max = 4). *Frame composition preserves the angular structure and nothing else* — the site-theoretic statement of the corpus's angular/radial seam (angular sparsity composes universally, Paper 22; the composition wall is radial, Paper 32's spatial-composition theorem).

**Join theorem (support + irreducibility).** Within each (l,m) block, the algebra generated by the two MASAs is the **full matrix algebra** M_d (connected support ⇒ irreducible *-algebra ⇒ Burnside), with d = n_max − l, so d ≥ 3 blocks appear at n_max ≥ 3 — and by Topos-1's machine-verified KS witness those blocks admit **no global valuation**. Composed radial observables therefore demand external valuations.

**The Family-1 mechanism:** externality = **meet-collapse followed by join-obstruction.** Family 2 reaches valuation-need ab initio (the inner factor is born noncommutative); Family 1 reaches it dynamically (two individually classical frames generate KS-obstructed algebra upon composition). Different routes, same terminus — precisely the two-family structure.

## 3. By-product: the rate-coincidence lemma (with its honest boundary)

At rate coincidence Z₁/n₁ = Z₂/n₂ both radial functions are Laguerre polynomials in the *same* variable; the extra power of x in the r² measure leaves exactly the tridiagonal band nonzero (the Track-H Laguerre S-matrix structure reappearing *across frames*):

- **(⇐, exhaustively verified Z′ ≤ 5, n ≤ 6, zero exceptions):** rate coincidence with |n₁ − n₂| ≥ 2 ⇒ exact zero overlap.
- **(⇒, panel-scoped):** on the panel every zero is a rate-coincidence zero; beyond it sporadic accidental zeros exist — the boundary case (Z′ = 2, l = 3, n = n′ = 5; rates 1/5 vs 2/5) is pinned in the test. Support connectivity — hence both theorems above — is unaffected either way.

*Process note:* my first statement of the lemma was the false biconditional (same-rate ⇔ zero); the backing test caught the overclaim before it reached the memo or paper (S(1s¹|2s²) = −1/4 is same-rate and nonzero). The corrected band-structure form then survived the widened sweep.

## 4. Honest scope

- **One instance of six.** The mixed-exponent pair is the Track-DF/radial-wall face of the multi-focal pattern. The two-center instance (Shibuya–Wulfman frames; expectation: only the m-grading survives the meet), and the mapping of all 17 Family-1 catalogue entries onto the mechanism, are the census follow-ons.
- The join theorem's Burnside step is standard finite-dimensional algebra (connected support ⇒ irreducibility ⇒ M_d); the computed content is the exact support connectivity.
- Exact pins: S(1s¹|1s²) = 2/27, S(1s¹|2s²) = −1/4, S(2p¹|2p²) = 256/81 (unnormalized).
- §13.5 untouched; nothing here bears on K's label.

## 5. Follow-ons

1. Two-center instance (m-only meet expected) — completes the two corpus-faithful faces.
2. The 17-entry Family-1 census against the mechanism.
3. General-parameter vanishing lemma (characterize the sporadic off-coincidence zeros).
4. (Inherited from Topos-1) full 60-entry classification; k = 2 site-reconstruction literature check.

## 6. Artifacts

Driver `debug/compute_topos2_family1_meetjoin.py` · data `debug/data/sprint_topos2_family1_meetjoin.json` · pins `tests/test_topos2_family1_meetjoin.py` (5/5, self-contained, exact) · Paper 57 §sec:open_bohr item 5 + honest-scope update.
