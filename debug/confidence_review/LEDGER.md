# GeoVac Confidence Review — Ledger

Per-paper confidence audit before external broadcast. Instruments:
`agents/CONFIDENCE_AUDITOR.md` + `agents/CITATION_CHECKER.md` — both now carry the
mandatory **cross-corpus check** and **verify-the-verifier (exact-text)** discipline.

Verdict key (per claim): A externally-verified / B internally-consistent-only /
C overstated / D unverifiable-here / E wrong.
Citation: OK / wrong-metadata / misattributed / doesn't-support / can't-find.
Broadcast color: GREEN / YELLOW / RED.

Discipline: verify-the-verify every consequential finding against the paper's
EXACT text before it drives an edit; check whether a flagged claim is corrected
elsewhere in the corpus (esp. Paper 18) before calling it a defect.

---

(Findings populated per run. Cleared 2026-06-01 for a fresh-model restart. The
two instrument lessons above are the only carry-forward; all prior discoveries
were removed by PI direction so the new model audits cold.)
