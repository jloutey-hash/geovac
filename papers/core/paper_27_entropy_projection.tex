\documentclass[twocolumn,pra,amsmath,amssymb,superscriptaddress]{revtex4-2}

\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{xcolor}

\begin{document}

\title{Entropy as a Projection Artifact:\\
One-Body Operators are Entanglement-Inert on Sparse Lattices}

\author{J.~Loutey}
\affiliation{Independent Researcher, Kent, Washington}

\date{April 14, 2026}

\begin{abstract}
We characterize the operator-theoretic origin of entanglement
entropy in the GeoVac Coulomb lattice, a finite spectral graph
realization of the hydrogenic angular momentum eigenbasis on
$S^{3}$.  Five results are presented.  First, the von Neumann
entropy of any single-particle reduced density matrix derived from
a \emph{non-degenerate} ground state of a purely one-body fermionic
Hamiltonian on a finite lattice is identically zero; numerically,
we confirm $S_{\mathrm{kin}}/S_{\mathrm{full}} \sim 10^{-14}$ for
He at $n_{\max}=2,3$ in the angular momentum eigenbasis.  All
ground-state entanglement is generated by the two-body interaction
$V_{ee}$, with the non-degeneracy qualifier essential: open-shell
and dissociated-bond states escape the floor.  Second, the
holographic area-law scaling $S_n = k\log A_n$ with
$A_n\propto n^{4}$, asserted on one-particle grounds in earlier
framework papers, is rigorously recovered as a pair-counting
statement: $A_n=g_n^{2}$, where $g_n=2n^{2}$ is the shell
degeneracy.  The factor of four is the two-body signature.
Third, in the Coulomb lattice the cusp concentrates as a single
``hot node'' on the $(1s,1s)$ pair-state of the $V_{ee}$ graph;
entanglement is co-located with cusp mass.  Fourth, on the
Bargmann--Segal harmonic oscillator lattice the ground-state
entropy of any two-fermion closed-shell state interacting via
\emph{any} central two-body potential is identically zero, a
consequence of Moshinsky--Talmi total-quanta conservation; this
is a rigidity statement structurally parallel to the Fock and
Bargmann--Segal theorems of Paper~24.  Fifth, Coulomb-lattice
block entropy obeys a universal scaling
$S_B = A\,(\tilde{w}_B/\delta_B)^{\gamma}$, where
$\tilde{w}_B$ is the dimensionless $V_{ee}$ mass off-diagonal
in the one-body eigenbasis and $\delta_B$ the dimensionless
first $H_1$ gap; the local exponent extrapolates to
$\gamma_{\infty} \approx 1.96$ in the joint $Z, n_{\max}\to\infty$
limit (Richardson on $n_{\max}=2,3,4,5$), slightly below the
non-degenerate second-order Rayleigh--Schr\"odinger value of
$2$, with the residual gap attributed to multi-shell
aggregation in the basis-extension limit.  Entropy is slotted into the
exchange-constant taxonomy as a projection-class observable:
zero on the combinatorial graph, finite only after the metric
embedding of $1/r_{12}$.
\end{abstract}

\maketitle

%=================================================================
\section{Introduction}
\label{sec:intro}
%=================================================================

The kinetic and potential parts of a fermionic Hamiltonian play
asymmetric roles in generating entanglement.  Slater determinants
exactly diagonalize one-body operators and carry only the
entanglement floor mandated by antisymmetrization; all
non-trivial orbital correlation arises from two-body terms.  This
fact is folklore in the quantum-chemistry and quantum-information
literatures (see, e.g., \cite{Peschel2003,LiHaldane2008}), but its
sharp form is rarely written out, and its consequences on
\emph{sparse, lattice-realized} Hamiltonians are seldom exhibited
explicitly.

The GeoVac framework~\cite{GeoVac_Paper0,GeoVac_Paper7,
GeoVac_Paper14} provides a controlled testbed: a finite spectral
graph on the angular momentum eigenbasis of $S^{3}$, where the
one-body operator $H_1$ is the graph Laplacian (with universal
coupling $\kappa = -1/16$) plus the diagonal hydrogenic spectrum,
and the two-body operator $V_{ee}$ is the Coulomb repulsion
$1/r_{12}$ projected onto the same vertex set.  Paper~26
established that, in this basis, the off-diagonal one-body
Hamiltonian carries $10$--$39\%$ of the correlation energy but
$<0.2\%$ of the single-orbital entanglement entropy across
He-like ions~\cite{GeoVac_Paper26}.  Paper~26 leaves open whether
this asymmetry is a quantitative numerical observation specific
to the basis at hand or a structural identity that holds at the
operator level.

This paper resolves the question.  Section~\ref{sec:operator}
states the operator-theoretic fact: on any finite lattice, the
one-body operator is entanglement-inert at the closed-shell
ground state.  Section~\ref{sec:numerical} confirms the result
to machine precision on He at $n_{\max}=2,3$.
Section~\ref{sec:area} re-derives the holographic area-law
$S_n \propto 4\log n$ as a pair-counting statement, fixing a
mislabeling in Paper~5~\cite{GeoVac_Paper5} where the scaling was
presented in one-particle language.  Section~\ref{sec:cusp}
shows that the cusp localizes as a single hot node on the
two-body $V_{ee}$ graph, making entropy and cusp mass spatially
co-located.  Section~\ref{sec:taxonomy} places entropy in the
exchange-constant taxonomy of Paper~18~\cite{GeoVac_Paper18} as
a projection-class observable.  Section~\ref{sec:predictions}
states two falsifiable predictions, one for the Bargmann--Segal
harmonic oscillator lattice~\cite{GeoVac_Paper24} and one
across the GeoVac molecular library.

%=================================================================
\section{Operator-Theoretic Result}
\label{sec:operator}
%=================================================================

\subsection{Setting}

Let $\mathcal{B} = \{\phi_1,\ldots,\phi_M\}$ be a finite
orthonormal single-particle basis on a lattice (the vertex set of
a spectral graph), and let
\begin{equation}
  H_1 \;=\; \sum_{ij} h_{ij}\, c_i^{\dagger} c_j,
  \qquad h = h^{\dagger},
  \label{eq:h1}
\end{equation}
be a one-body Hermitian operator on the antisymmetric Fock space
generated by $\mathcal{B}$.  Let $\Phi_0$ be the ground state of
$H_1$ at fixed particle number $N$.

\subsection{Statement}

\noindent\textbf{Proposition.} \emph{If the spectrum of $h$ is
non-degenerate at the Fermi level (i.e., $\Phi_0$ corresponds to
a uniquely defined Slater determinant of the $N$ lowest natural
orbitals of $h$), then for any subset
$A \subset \mathcal{B}$ of orbitals the von Neumann entropy
\begin{equation}
  S_A \;=\; -\mathrm{Tr}\,\bigl[\rho_A \log \rho_A\bigr],
  \qquad
  \rho_A \;=\; \mathrm{Tr}_{\mathcal{B}\setminus A}\,
  \lvert\Phi_0\rangle\langle\Phi_0\rvert,
\end{equation}
is determined entirely by the natural-orbital occupation
spectrum of $\Phi_0$ restricted to $A$, which consists of zeros
and ones in the natural-orbital basis.  Equivalently, in the
basis that diagonalizes $h$, $\rho_A$ is a tensor product of
single-mode density matrices each of which has support on a
single occupation eigenvalue.  In a generic basis $\mathcal{B}$,
the resulting entropy is the antisymmetrization (Slater)
entropy~\cite{GhirardiMarinatto2004}, and is independent of
$h$.}

\medskip
The content of the proposition is well known in
quantum-information formulations of free-fermion systems
\cite{Peschel2003}: a Slater determinant has a Gaussian
correlation matrix, and its reduced density matrices are
fully characterized by the projector $C = P_A^{\dagger} P P_A$
onto the occupied subspace, restricted to the partition $A$.
The eigenvalues of $C$ are pinned to $\{0,1\}$ in the natural
basis; any positive entropy of $\rho_A$ in a different basis is
a basis-projection artifact, not a physical correlation.

\subsection{Scope of the proposition}
\label{sec:scope}

The non-degeneracy clause is essential.  An empirical scan
(EP-2j) at $n_{\max}=3$ in the GeoVac S$^{3}$ basis with
$N=3,4$ electrons shows two distinct violation modes:

\emph{Open-shell single Slater determinant (Li, $^{2}$S
ground state).}  The H$_{1}$ ground state is non-degenerate
(gap $\sim 10^{-5}$ Ha) and is exactly one Slater determinant
$|1s^{2}\,2s\rangle$, so the proposition's premise holds in
the canonical statement of Sec~II.  However, the spatial
1-RDM has eigenvalues $(2, 1, 0, \ldots)$, and the von
Neumann entropy of the normalized 1-RDM
$S = -\sum_i (n_i/N)\log(n_i/N) = -\tfrac{2}{3}\log\tfrac{2}{3}
- \tfrac{1}{3}\log\tfrac{1}{3} \approx 0.637$ nats is nonzero
not from V$_{ee}$ correlation but from the open-shell
\emph{antisymmetrization} of the single SD.  The V$_{ee}$
correlation contribution is the small differential
$S_{\mathrm{full}} - S_{\mathrm{kin}} \approx 0.010$ nats at
$Z=3$.  The proposition's S$_A$=0 statement applies in the
\emph{natural-orbital basis of $h$} where the open-shell SD
has occupation $(1, 1, 1, 0, \ldots)$ in spin-orbital units,
giving $S=0$ trivially in that basis.  In the spatial 1-RDM,
the antisymmetrization entropy is the relevant floor, not
zero.

\emph{Genuine H$_{1}$ degeneracy (Be, $^{1}$S ground state at
$n_{\max}=3$).}  The bare graph H$_{1}$ has a 3-fold
degenerate ground state from the unbroken $2s/2p$ shell
degeneracy (the diagonal $-Z^{2}/2n^{2}$ does not split
$2s$ from $2p$, and the $\kappa$-adjacency does not lift it
in the $M_L=0, M_S=0$ sector).  The H$_{1}$-only ground
ensemble is mixed, the spatial 1-RDM has fractional
occupations $(2, 0.76, 0.76, 0.47, 0, \ldots)$, and
$S_{\mathrm{kin}} = 1.23$ nats $\neq 0$.  V$_{ee}$ lifts the
degeneracy and \emph{reduces} entropy:
$S_{\mathrm{full}} = 0.902 < S_{\mathrm{kin}}$.  Here the
proposition's premise fails outright; the floor is
$S_{\mathrm{kin}}>0$, not zero.

The He two-electron case satisfies the proposition's premise
in the strictest form: $1s^{2}$ closed shell exhausts the
non-degenerate $n=1$ shell, the GS is a single SD, the
spatial 1-RDM has eigenvalues $(2, 0, \ldots)$, and the
floor is $S_{\mathrm{kin}}=0$.  Generically the
proposition's spatial-basis floor $S_{\mathrm{kin}}=0$
applies to closed-shell ground states whose electron count
exhausts an entire $(n,l)$-degenerate sub-shell at the
$h$-eigenbasis level.  For the GeoVac S$^{3}$ basis at
$n_{\max}\geq 2$, this restricts the strict floor to
He-like atoms and to systems whose closed shells fall
entirely in the lowest $1s$ sub-shell.  The proposition's
operator content is general; its quantitative spatial-basis
realization narrows.

\paragraph*{Analytical degenerate perturbation theory for Be
(EP-2N).}  The Be case admits a clean analytical treatment.
At $n_{\max}=2$, $M_L=M_S=0$, the H$_1$ ground subspace
is exactly $3$-dimensional, spanned by
$\{|1s^{2}2s^{2}\rangle,\, |1s^{2}2p_{0}^{\,2}\rangle,\,
|1s^{2}\,(2p_{-1}2p_{+1})_{S=0}\rangle\}$ at common
$E_{\mathrm{kin}} = -5Z^{2}/4$.  Diagonalizing V$_{ee}$ within
this subspace (a $3\times 3$ degenerate-PT problem) at $Z=4$
yields a ground state $0.98\,|2s^{2}\rangle - 0.14\,|2p_{0}^{\,2}\rangle
- 0.14\,|(2p_{-1}2p_{+1})_{S=0}\rangle$ -- $96\%$ pure
$2s^{2}$ with $\sim 2\%$ admixture each of the two
$2p$-pair configurations.  The analytical spatial 1-RDM has
diagonal occupations $(2.000, 1.924, 0.020, 0.037, 0.020)$ in
the $(1s, 2s, 2p_{-1}, 2p_{0}, 2p_{+1})$ basis, giving
$S_{\mathrm{full}} = 0.794$ nats vs.\ the equal-mixture
$S_{\mathrm{kin}} = 1.358$ nats.  V$_{ee}$ thus acts as a
\emph{degenerate-PT lifter}: it selects the $2s^{2}$
configuration and reduces entropy by $42\%$.  The full FCI
calculation at $n_{\max}=3$ (EP-2j) gives $S_{\mathrm{full}}
= 0.902$ in qualitative agreement, with the additional
shells contributing modest extra correlation entropy on top of
the analytical degenerate-PT prediction.  This sharpens the
post-degeneracy floor to a calculable quantity rather than
the trivial $S_{\mathrm{kin}}=0$ of the non-degenerate case.

\subsection{GeoVac specialization}

For the GeoVac Coulomb lattice, $\mathcal{B}$ is the angular
momentum eigenbasis $\{\phi_{nlm}\}$ on $S^{3}$.  In this basis
the one-body operator is
\begin{equation}
  H_1 \;=\; \mathrm{diag}(\varepsilon_{nlm})
  \;+\; \kappa\, L,
  \qquad \kappa = -\tfrac{1}{16},
  \label{eq:h1_geovac}
\end{equation}
where $\varepsilon_{nlm} = -Z^{2}/(2n^{2})$ is the hydrogenic
spectrum and $L$ is the GeoVac graph Laplacian.  $H_1$ is
non-degenerate at the closed-shell He ground state for all
$n_{\max}\geq 1$.  By the proposition, the single-orbital
entropy of the $H_1$-only ground state vanishes in the natural
orbital basis.  In the angular momentum basis, $H_1$ is nearly
but not exactly diagonal, and the natural orbitals deviate from
$\{\phi_{nlm}\}$ by $O(|\kappa|/Z^{2})$.  The single-orbital
entropy in $\mathcal{B}$ inherits an exponentially small
projection contribution that is bounded above by the deviation
of $\mathcal{B}$ from the natural-orbital basis of $h$.

%=================================================================
\section{Numerical Confirmation}
\label{sec:numerical}
%=================================================================

We solve for the ground state of two He Hamiltonians on the
$S^{3}$ angular momentum eigenbasis at $n_{\max}\in\{2,3\}$:
\begin{itemize}
  \item the full Hamiltonian
  $H_{\mathrm{full}} = H_1 + V_{ee}$, with $V_{ee}$ from the
  exact-rational hypergeometric Slater integral
  evaluator~\cite{GeoVac_Paper7,EP1_data};
  \item the kinetic-only Hamiltonian
  $H_{\mathrm{kin}} = H_1$, identical to $H_{\mathrm{full}}$
  with $V_{ee}\equiv 0$.
\end{itemize}
We diagonalize each in the $M_L=0$ singlet sector by full
configuration interaction, build the spatial 1-RDM, and report
its von Neumann entropy in nats.

Table~\ref{tab:ep1} reproduces the EP-1 data
file~\cite{EP1_data}.

\begin{table}
\caption{One-body vs.\ full ground-state entropy for He on the
$S^{3}$ angular lattice.  $S_{\mathrm{full}}$ and
$S_{\mathrm{kin}}$ are the single-orbital von Neumann entropies
of the FCI ground states of $H_{\mathrm{full}}$ and
$H_{\mathrm{kin}}$, respectively.  Data from
\texttt{debug/data/ep1\_entropy\_partition.json}~\cite{EP1_data}.
\label{tab:ep1}}
\begin{ruledtabular}
\begin{tabular}{cdddd}
$n_{\max}$ & \multicolumn{1}{c}{$E_{\mathrm{full}}$ (Ha)}
            & \multicolumn{1}{c}{$S_{\mathrm{full}}$ (nats)}
            & \multicolumn{1}{c}{$S_{\mathrm{kin}}$ (nats)}
            & \multicolumn{1}{c}{$S_{\mathrm{kin}}/S_{\mathrm{full}}$} \\
\hline
2 & -2.8895 & 0.03955 & -2.2\times 10^{-16} & -5.6\times 10^{-15} \\
3 & -2.8931 & 0.04081 &  7.8\times 10^{-16} &  1.9\times 10^{-14} \\
\end{tabular}
\end{ruledtabular}
\end{table}

The kinetic-only entropy is at the floating-point noise floor
($\sim 10^{-16}$ nats), confirming the proposition to machine
precision.  The full Hamiltonian's entropy
($S_{\mathrm{full}} \sim 4\times 10^{-2}$ nats) is fourteen
orders of magnitude larger.  The natural-orbital occupations of
the kinetic-only ground state at $n_{\max}=3$ are
$(2.000, 8.3\times 10^{-22}, 0, 0,\ldots)$, exactly the
$\{0,1\}$ structure (here doubled by spin) predicted by the
proposition.  The full Hamiltonian's occupations
$(1.988, 0.00896, 0.00093, 0.00093,\ldots)$ exhibit the
$O(10^{-3})$ deviations from $\{0,2\}$ characteristic of the
two-body $V_{ee}$ correlation.

The result is expected.  Its value lies in making it visible on
the concrete Coulomb lattice and in demonstrating that the
floor is operator-theoretic, not basis-fitted.

%=================================================================
\section{Area Law as Pair Counting}
\label{sec:area}
%=================================================================

\subsection{The Paper~5 statement}

The framework synthesis paper~\cite{GeoVac_Paper5} states
(Section~``Entropy and the second law'', verbatim):
\begin{quote}
The holographic entropy of shell $n$ is
$S_n = k\ln A_n + \mathrm{const}$, where $A_n \propto n^{4}$
is the ``surface area'' (number of plaquettes).
\end{quote}
The supporting microstate count was not given.  The shell
degeneracies in Paper~0~\cite{GeoVac_Paper0} are $g_n^{\mathrm{orb}}=n^2$
(orbital, no spin) and $g_n=2n^{2}$ (with spin); cumulative state
count is $N(n)=n(n+1)(2n+1)/3 \sim (2/3)n^{3}$.  Neither yields
$n^{4}$.

\subsection{Pair counting on shell $n$}

Define a \emph{plaquette} on shell $n$ as an ordered pair of
sites,
\begin{equation}
  A_n \;\equiv\; g_n^{2} \;=\; 4\, n^{4}.
  \label{eq:An}
\end{equation}
Equivalently, the unordered antisymmetrized pair count is
\begin{equation}
  \tfrac{1}{2}\, g_n(g_n-1) \;=\; n^{2}(2n^{2}-1)
  \;=\; 2\,n^{4} + O(n^{2}),
\end{equation}
and the adjacent-shell pair count is
$g_n g_{n-1} = 4n^{2}(n-1)^{2} = 4n^{4} + O(n^{3})$.  All three
two-body counts agree at leading order:
\begin{equation}
  \log A_n \;=\; 4\,\log n \;+\; \mathrm{const}.
  \label{eq:logAn}
\end{equation}
A factor of four arises only at the two-body level: each
electron contributes $\log g_n = 2\log n + \log 2$, and the two
factors compose multiplicatively.  At the one-body level,
single-shell uniform entropy is $\log g_n = 2\log n + \log 2$
(off by a factor of two), and cumulative entropy
$\log N(n) \sim 3\log n$ (off by a factor of $4/3$).  These are
exhibited explicitly in the EP-3 analysis~\cite{EP3_data}.

The area-law scaling $A_n \propto n^{4}$ asserted in Paper~5
is therefore correct, but it is a \emph{two-body} statement.  In
the operator-theoretic language of Section~\ref{sec:operator},
the area law lives on the pair graph of the $V_{ee}$ operator,
not on the single-particle vertex set of $H_1$.

\subsection{Reconciliation}

Paper~5 is a predecessor framework synthesis with conjectural
status.  Its area-law claim is rigorously recovered here as a
pair statement.  Two consequences follow:
\begin{enumerate}
  \item The area law cannot be derived from one-particle
    microstate counting on the GeoVac packing lattice.  Any
    interpretation that assigns the $n^{4}$ scaling to a
    one-particle observable is mislabeled.
  \item The information-theoretic content of the scaling is
    consistent with Section~\ref{sec:operator}: entropy on the
    GeoVac lattice is generated by the two-body operator, and
    its leading shell scaling counts pair states.
\end{enumerate}

%=================================================================
\section{Cusp as Entropy Concentration}
\label{sec:cusp}
%=================================================================

The energy-graph characterization in v2.9.1~\cite{EnergyGraph}
constructs the singlet pair-state graph
$\{\lvert(ab)\rangle\}$ at $n_{\max}=3$ ($N=31$ nodes) and
$n_{\max}=4$ ($N=101$ nodes), with edge weights
$V_{IJ} = \langle (ab)\lvert 1/r_{12}\rvert (cd)\rangle$
computed from the exact-float hypergeometric Slater
integrals~\cite{GeoVac_Paper7}.  Two findings of that
characterization bear directly on the operator-theoretic
result of Section~\ref{sec:operator}.

\paragraph*{Frobenius-mass diagonality.}  In the eigenbasis of
the one-body operator $H_1$, $V_{ee}$ is overwhelmingly
diagonal:
\begin{equation}
  \frac{\lVert \mathrm{diag}_{H_1}\!V_{ee}\rVert_F}
       {\lVert V_{ee}\rVert_F}
  \;=\; 0.920 \text{ (}n_{\max}=3\text{)},
  \;\; 0.892 \text{ (}n_{\max}=4\text{)},
\end{equation}
using the unsquared Frobenius ratio (squared values are $0.847$
and $0.795$).  The relative commutator
$\lVert [H_1, V_{ee}]\rVert_F / \lVert H_1 \rVert_F = 6.1\%$ at
$n_{\max}=3$, dropping only to $5.3\%$ at $n_{\max}=4$.  The
commutator does not vanish with basis size; $H_1$ and $V_{ee}$
are genuinely non-commuting.  The wavefunction-graph eigenbasis
``almost'' diagonalizes the entropy-generating operator, and the
residual $\sim 10\%$ off-diagonal mass is precisely the cusp
correlation that drives convergence from $\sim 3\%$ to $0.2\%$
in graph-native CI.

\paragraph*{Hot-node localization.}  On the same pair-state
graph, the $(1s,1s)$ vertex is simultaneously the
maximum-diagonal entry ($V_{(1s,1s),(1s,1s)} = 5/8$, $42\%$
of the next-largest diagonal), the largest weighted hub, and
the head of the largest off-diagonal edge
$(1s,1s)\leftrightarrow(1s,2s)$.  The cusp does not distribute
across pair-states; it concentrates on the unique pair-state
where both electrons can simultaneously occupy small-$r$
regions.  Combined with Section~\ref{sec:operator}, this
identifies the cusp not only as the energy-evaluation
bottleneck~\cite{GeoVac_Paper18} but as the locus of
entanglement mass: the pair-state with maximal $V_{ee}$
contribution is also the pair-state on which the
two-body-generated entropy concentrates.

%=================================================================
\section{Exchange-Constant Taxonomy Placement}
\label{sec:taxonomy}
%=================================================================

Paper~18~\cite{GeoVac_Paper18} classifies observables by the
type of transcendental content their evaluation requires:
\emph{intrinsic} (rational graph-native quantities, e.g.\ Slater
$F^{0}$ values), \emph{calibration} (transcendental constants of
the conformal projection, e.g.\ $\pi$ from $S^{3}$ Weyl
densities), \emph{embedding} (constants that enter when the
graph is mapped to a continuous metric, e.g.\ the Coulomb
$1/r_{12}$ kernel), and \emph{flow} (renormalization-style
constants, e.g.\ Phillips--Kleinman).

Entropy fits the framework cleanly as a
\emph{projection-class observable}:
\begin{itemize}
  \item On the bare combinatorial graph (vertex set
    $\{\phi_{nlm}\}$, one-body operator $H_1$ alone), the
    ground-state entropy is identically zero
    (Section~\ref{sec:operator}).  No transcendental enters.
  \item Under the metric embedding of $1/r_{12}$ onto the
    pair-state graph, entropy becomes finite.  Diagonal pair
    entries $V_{ii}$ remain rational
    (Section~\ref{sec:cusp}, \cite{EnergyGraph}); spectral
    eigenvalues of $V_{ee}$ on the pair graph are irrational,
    consistent with the embedding-class signature of $1/r_{12}$
    in Paper~18.
\end{itemize}
Entropy is therefore an embedding-class observable in the
Paper~18 taxonomy.  The combinatorial graph is entropy-blind;
entropy is the price of projecting onto the metric continuum.

This placement is consistent with the Paper~24 HO rigidity
theorem~\cite{GeoVac_Paper24}: the Bargmann--Segal harmonic
oscillator lattice is bit-exactly $\pi$-free in rational
arithmetic, and its one-body Hamiltonian has the same
entanglement-inert character as $H_1$ here (it is one-body by
construction).  Whether two-fermion entanglement on the HO
lattice carries the same area-law scaling as the Coulomb case
is the subject of Section~\ref{sec:predictions}.

%=================================================================
\section{Two Rigidity Results}
\label{sec:predictions}
%=================================================================

This section states the two main entropy results beyond
Section~\ref{sec:operator}'s operator-theoretic floor.
Section~\ref{sec:pred1} establishes a rigidity theorem for the
Bargmann--Segal harmonic-oscillator lattice: two-fermion
closed-shell ground-state entropy is zero for any central
two-body potential.  Section~\ref{sec:pred2} characterizes a
universal two-variable scaling for Coulomb-lattice block
entropy, asymptotic to second-order Rayleigh--Schr\"odinger
perturbation theory.  Both were originally phrased as
speculative predictions; both are now established
numerically, with Section~\ref{sec:pred1} strengthened to a
structural statement by Moshinsky--Talmi total-quanta
conservation.

\subsection{HO zero-entropy theorem for closed-shell
            two-fermion ground states}
\label{sec:pred1}

\textbf{Outcome (EP-2b, this work).}  The prediction resolves
cleanly: $S_{\mathrm{HO}} = 0$ \emph{identically} at the ground
state, not merely $\ll S_{\mathrm{Coulomb}}$.  The mechanism is
stronger than the ``short-range kernel suppresses cusp
concentration'' heuristic originally stated: on the
Bargmann--Segal HO basis, the one-body operator $H_{HO}$
commutes exactly with \emph{any} central two-body interaction
$V(r_{12})$ because the Moshinsky--Talmi transformation makes
total HO quanta $N_1+N_2=N_{\mathrm{rel}}+N_{CM}$ a conserved
quantum number.  Consequently the ground state lies in the
lowest-$N_{\mathrm{tot}}$ sector of $H_{HO}$; for a
closed-shell system (two fermions in $M_L=0$, $M_S=0$), that
sector is one-dimensional, and the ground state is a single
Slater determinant $|(0s)^{2}\rangle$.

\paragraph*{Numerical confirmation (EP-2b).}  With the
Minnesota NN singlet-channel kernel at $\hbar\omega=10\,\mathrm{MeV}$:

\begin{table}[h]
\caption{EP-2b: two-fermion HO on the Bargmann--Segal lattice,
Minnesota $S=0$ interaction.  $E_{\mathrm{full}}$ is the FCI
ground-state energy; $E_{\mathrm{kin}}$ is the noninteracting
(HO-only) ground-state; $S_{\mathrm{full}}$ is the spatial
1-RDM von Neumann entropy of the interacting ground state.
Data from \texttt{debug/data/ep2b\_ho\_two\_fermion.json}.}
\label{tab:ep2b}
\begin{ruledtabular}
\begin{tabular}{cdddd}
$N_{\max}$ & \multicolumn{1}{c}{$E_{\mathrm{full}}$ (MeV)}
            & \multicolumn{1}{c}{$E_{\mathrm{kin}}$ (MeV)}
            & \multicolumn{1}{c}{$S_{\mathrm{full}}$ (nats)}
            & \multicolumn{1}{c}{$\lVert[H_{HO},V]\rVert_F/\lVert H_{HO}\rVert_F$} \\
\hline
2 &  14.898 & 30.000 & 0.0 & 7.9\times 10^{-16} \\
3 &  14.898 & 30.000 & 0.0 & 4.4\times 10^{-16} \\
\end{tabular}
\end{ruledtabular}
\end{table}

The FCI ground state at both basis sizes is bit-identical to
the single Slater determinant $|(0s)^{2}\rangle$; top-$4$
natural-orbital occupations are $(2.0, 0, 0, 0)$ exactly.  The
$N_{\max}=2\to 3$ comparison shows that enlarging the single-
particle basis does \emph{not} mix higher shells into the
ground state: the commutator $[H_{HO}, V]$ is at the
floating-point noise floor, so all eigenvectors of $H_{\mathrm{full}}$
are eigenvectors of $H_{HO}$, and the ground state is locked
to the lowest-$N_{\mathrm{tot}}$ irreducible block.

\paragraph*{Interpretation.}  The operator-theoretic statement
of Section~\ref{sec:operator} should be read in its sharp form:
entanglement is generated by the component of the two-body
operator \emph{off-diagonal in the $H_1$ eigenbasis restricted
to the ground-state sector}, not merely by $V$'s total Frobenius
mass.  On the HO lattice, $V$ has nonzero total Frobenius mass
(and even a $\sim 93\%$ diagonality ratio in the $H_{HO}$
eigenbasis that parallels the Coulomb number), but its
off-diagonal component between $N_{\mathrm{tot}}$ eigenspaces is
\emph{exactly zero}, so the ground state does not mix, and
$S=0$ exactly.  The Coulomb kernel, in contrast, does not
conserve any HO-like $N_{\mathrm{tot}}$ quantum number; it
couples across shells (the $6$--$8\%$ off-diagonal residue
documented in Section~\ref{sec:cusp}) and generates the
$S\sim 4\times 10^{-2}$ nats observed in EP-1.

\paragraph*{Structural content.}  The result generalizes beyond
Minnesota to \emph{any} central two-body $V(r_{12})$ on the 3D
isotropic HO basis: total-quanta conservation is a property of
the Moshinsky--Talmi decomposition, not of the specific kernel.
This is a discrete quantum-information reading of a standard
nuclear-structure fact~\cite{Moshinsky1996}.  It provides a
zero-entropy baseline for the Paper~18 exchange-constant
taxonomy: on the HO graph, entropy is not merely
``embedding-class with small amplitude'' but vanishes
identically at closed-shell ground states, consistent with the
HO rigidity theorem of Paper~24~\cite{GeoVac_Paper24}
($\pi$-free graph certificate extends to $\pi$-free ground
entropy).

\paragraph*{Implementation.}  The experiment is driven by
\texttt{debug/ep2b\_ho\_two\_fermion.py} using the new
\texttt{geovac.nuclear.ho\_two\_fermion} module (which
composes the existing
\texttt{geovac.nuclear.bargmann\_graph},
\texttt{geovac.nuclear.moshinsky}, and
\texttt{geovac.nuclear.minnesota} infrastructure from
Paper~23~\cite{GeoVac_Paper23}).  The numerical claims above
are pinned by
\texttt{tests/test\_paper27\_entropy.py::test\_paper27\_ep2b\_ho\_gs\_is\_single\_determinant}
and
\texttt{::test\_paper27\_ep2b\_ho\_kinetic\_interaction\_commute}.

\subsection{Universal scaling of Coulomb block entropy
            (\textbf{verified, reframed})}
\label{sec:pred2}

\paragraph*{Main result.}  Block ground-state entropy on the
Coulomb lattice obeys a universal two-variable scaling in the
dimensionless off-diagonal $V_{ee}$ mass
$\tilde{w}_B \equiv \lVert V_{ee}^{\mathrm{off-diag}(H_1)}\rVert_F
/ \lVert H_1 \rVert_F$ and the dimensionless first $H_1$ gap
$\delta_B \equiv \Delta E_1 / \lVert H_1 \rVert_F$:
\begin{equation}
  S_B \;=\; A\,(\tilde{w}_B / \delta_B)^{\gamma}.
  \label{eq:pred2_universal}
\end{equation}
The local exponent approaches a basis-extension limit
$\gamma_{\infty} \approx 1.96$ as both $Z \to \infty$ and
$n_{\max}\to\infty$ (Richardson extrapolation on $n_{\max}=2,3,4,5$
local slopes at $Z=15\to 30$).  This is \emph{below} the
non-degenerate second-order Rayleigh--Schr\"odinger prediction
$\gamma=2$.  The persistent gap of $\sim 0.04$ at the basis
limit is an aggregation effect: with more orbitals available,
$S_B$ accumulates contributions from many small admixtures
rather than being dominated by the lowest excitation, and the
aggregate sum scales with a slightly smaller exponent.
Finite-window fits give $\gamma \in [2.1, 2.4]$, depending
monotonically on both $Z$ and $n_{\max}$.  Across $22$ calibration points
(He-like $Z\in[2,100]$ at $n_{\max}=3$, plus a LiH bond-block
$R$-sweep from $1.5$ to $8$ bohr) Eq.~\eqref{eq:pred2_universal}
fits at $R^{2}=0.990$ with $\gamma = 2.228$, $A = 0.157$; the
only points with $>25\%$ deviation are the dissociation-limit
$R\in\{6,8\}$ bohr LiH-bond points that saturate at
$S_B = \log 2$ (the two-site ceiling).

\paragraph*{Numerical confirmation.}  Three calibrations pin
Eq.~\eqref{eq:pred2_universal}.  (i) He-like isoelectronic
sequence at $n_{\max}=3$, $Z\in\{2,\ldots,10\}$ gives
$\gamma = 2.383$, $A = 8.16$, $R^{2}=0.998$ in the single-variable
form $S_B = A\tilde{w}_B^{\gamma}$; this is equivalent to
Eq.~\eqref{eq:pred2_universal} because $\delta_B$ is essentially
constant at $0.20$ across the He-like family.  The companion
$Z$-scaling fit $S_B\sim Z^{\alpha_Z}$ gives
$\alpha_Z = -2.613$, $R^{2}=0.995$, consistent with
Paper~26's single-orbital entropy
$\alpha_Z = -2.56$~\cite{GeoVac_Paper26}.
(ii) Extending to $Z\in\{12, 15, 20, 30, 50, 100\}$ gives a
monotonically decreasing local log-log slope:
$\gamma_{\mathrm{loc}}(Z=2) = 2.64 \to
\gamma_{\mathrm{loc}}(Z=100) = 1.92$, crossing $2$ between
$Z=10$ and $Z=12$.  The underlying $S_B(\tilde{w}_B/\delta_B)$
curve is smooth, not a pure power law; local linearization over
any finite window picks up a slope that depends on the window.
(iii) Fixing the Z-window $Z\in\{2,\ldots,30\}$ and sweeping
$n_{\max}\in\{2,3,4\}$ gives local slopes at large Z of $1.992$,
$1.976$, and $1.966$ respectively: both $Z$ and $n_{\max}$ drive
$\gamma$ toward $2$ from above.

\begin{table}[h]
\caption{EP-2c pilot data: He-like isoelectronic sequence at
$n_{\max}=3$.  Full data in
\texttt{debug/data/ep2c\_entropy\_vs\_vee\_offdiag.json}.
$\delta_B \approx 0.20$ (constant to $1\%$) across the entire
table.}
\label{tab:ep2c}
\begin{ruledtabular}
\begin{tabular}{cdd}
$Z$ & \multicolumn{1}{c}{$S_B$ (nats)}
    & \multicolumn{1}{c}{$\tilde{w}_B$} \\
\hline
2  & 4.08\times 10^{-2} & 0.1052 \\
3  & 1.12\times 10^{-2} & 0.0648 \\
4  & 4.98\times 10^{-3} & 0.0470 \\
6  & 1.82\times 10^{-3} & 0.0306 \\
8  & 9.48\times 10^{-4} & 0.0226 \\
10 & 5.86\times 10^{-4} & 0.0179 \\
\end{tabular}
\end{ruledtabular}
\end{table}

\paragraph*{Methodological robustness.}  Three potential
confounders were checked and did not affect the main result.
(a) The apparent $40\%$ prefactor offset between ``core'' and
``lone-pair'' blocks in an earlier 16-block multi-block fit is a
pure Z-range artifact; repeating the same fit on the
\emph{identical} He-like family restricted to the core and
lone-pair Z-sets reproduces the offset without any block-type
variation.  The underlying curve is not a pure power law, and
log--log regression over different Z-windows picks up different
local slopes (finite-basis residue at small Z inflates
$\gamma$).  (b) A bounded ansatz
$S_B = \log 2 \cdot \tanh^{2}(c\,(\tilde{w}_B/\delta_B)^{\gamma})$,
motivated by the two-site $\log 2$ ceiling, fits markedly worse
than the pure power law ($R^{2}=0.37$ vs.\ $0.90$ on a
21-point mixed-geometry set) because the tanh Taylor expansion
forces the small-argument slope to $2\gamma$ with a small
prefactor $c^{2}$, incompatible with the $\sim 4$-decade
dynamic range in $S_B$.  Three additional bounded ansatze
(rational saturation, stretched exponential, Hill function)
were tested in log-space on a combined $32$-point dataset
(EP-2g $+$ EP-2k HF and H$_2$O bonds) and all give
$R^{2}_{\mathrm{log}} \in [0.985, 0.989]$, statistically
indistinguishable from the pure power law's $0.9895$.  The
saturation at $\log 2$ is \emph{discrete}, not smooth: only
one or two LiH dissociation-limit points actually reach the
ceiling, so saturation parameters cannot earn their fit cost.
The pure power law Eq.~\eqref{eq:pred2_universal} remains the
best single functional form.  (c) The $n_{\max}=2,3,4$ sequence of
global-window exponents $(\gamma, A) = (2.63, 0.73),
(2.38, 0.18), (2.32, 0.09)$ shows monotone approach to $\gamma=2$
from above, at fixed Z-window $\{2,3,4,6,10\}$; the local slope
at the large-Z edge ($Z=15\to 30$) approaches $\gamma=2$ more
directly (Table~\ref{tab:gamma_surface}).

\begin{table}[h]
\caption{$\gamma(Z, n_{\max})$ surface (EP-2i + EP-2L).  The
local slope of $S_B$ vs.\ $\tilde{w}_B/\delta_B$ between $Z=15$
and $Z=30$ decreases monotonically with $n_{\max}$, crossing
$2$ between $n_{\max}=2$ and $3$.  Richardson extrapolation
on $n_{\max}=3,4,5$ gives an asymptotic slope
$\gamma_{\infty} \approx 1.96$, \emph{below} the
non-degenerate Rayleigh--Schr\"odinger value of $2$.  The
global-window $\gamma$ over $Z\in\{2,3,4,6,10,15,30\}$ is
dominated by small-$Z$ finite-basis residue and is consistently
higher.}
\label{tab:gamma_surface}
\begin{ruledtabular}
\begin{tabular}{cdd}
$n_{\max}$
  & \multicolumn{1}{c}{$\gamma$ (global, $Z \in [2,30]$)}
  & \multicolumn{1}{c}{$\gamma_{\mathrm{loc}}$ ($Z:15\to 30$)} \\
\hline
2 & 2.373 & 1.992 \\
3 & 2.223 & 1.976 \\
4 & 2.182 & 1.966 \\
5 & --    & 1.959 \\
\hline
$\infty$ (extrapolated) & --- & $\approx 1.96$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

\paragraph*{Heavy-atom R-independence (EP-2k).}  HF and H$_2$O
bond blocks were R-swept across $R\in[1.3, 3.0]$ bohr (HF) and
$R_{OH}\in[1.5, 3.0]$ bohr (H$_2$O).  Both are essentially
\emph{R-independent}: $S_B$ varies by $0.4\%$ for HF and
$0.2\%$ for H$_2$O across the entire range.  At every R-point
HF sits at $S_B/S_{\mathrm{predicted}} = 1.20\pm 0.02$ above
Eq.~\eqref{eq:pred2_universal}, and H$_2$O at
$1.25\pm 0.01$.  In contrast, LiH varies by $70\times$ across
the same R-range and shows the saturation at $\log 2$
discussed above.

The mechanism is the Z asymmetry between center and partner.
HF (Z$_{\mathrm{F}}=9$, Z$_{\mathrm{H}}=1$) and H$_2$O
(Z$_{\mathrm{O}}=8$, Z$_{\mathrm{H}}=1$) have bonds dominated
by the heavy atom; the H-side orbital contributes a
perturbative correction whose amplitude depends on R only
through cross-center $V_{ne}$ matrix elements that vary
slowly compared to the heavy-atom-dominated $H_1$ structure.
LiH (Li-side Z$_{\mathrm{eff}}=1=$ H-side Z) is the
symmetric special case where bond stretching genuinely
populates two equal-charge centers, driving the
quasi-degenerate regime and the eventual $\log 2$ saturation.
For chemically realistic heavy-atom bonds at or near
equilibrium, the universal curve holds within a $25\%$ band
that is R-independent within $\sim 1\%$.

\paragraph*{Bond-block qualifier.}  For bond blocks with
two-center $H_1$, $\delta_B$ is the \emph{essential} second
variable: the LiH bond-block point at equilibrium
($\tilde{w}_B = 0.042$) would give $S_B \approx 4\times 10^{-3}$
under the single-variable fit but the actual value is $0.079$,
a $20\times$ deviation.  Including $\delta_B$ collapses it onto
Eq.~\eqref{eq:pred2_universal} within the universal band.
Bond blocks in isolation, however, are not a single
well-defined object: the composed LiH bond block (within-block
Hamiltonian only, Li-side $Z_{\mathrm{eff}}=1 = $ H-side $Z$)
is \emph{exactly degenerate} ($\delta_B=0$) because the two
sub-block 1s orbitals share the same effective charge.  Only
the cross-center $V_{ne}$ terms introduced by the balanced
builder~\cite{GeoVac_Paper19} break the degeneracy and put the
bond block at a finite $(\tilde{w}_B,\delta_B)$.  A similar
mechanism explains why BeH$_2$ at equilibrium saturates at
$\log 2$ in the balanced builder: cross-block ERIs from the
Be-core and the second Be--H bond compress $\delta_B$ from
$0.204$ (within-block) to $0.027$ (balanced), an $8\times$
reduction that crosses into the saturation regime.  By
contrast, the H$_2$O bond block shows no measurable shift
($S_{\mathrm{balanced}}/S_{\mathrm{composed}} = 1.002$), and its
factor-of-$2$ residual from the universal-curve prediction is
intrinsic.  The operational statement is:
Eq.~\eqref{eq:pred2_universal} applies when one chooses a
specific $H_1$ (including its cross-block contributions) and
tracks both $\tilde{w}_B$ and $\delta_B$ for that choice.

\paragraph*{Implementation.}  Drivers:
\texttt{debug/ep2c\_entropy\_vs\_vee\_offdiag.py} (single-block
pilot), \texttt{ep2c\_multi\_block.py} (multi-block),
\texttt{ep2d\_zrange\_control.py} (Z-range control),
\texttt{ep2f\_full\_library.py} (library + high-Z),
\texttt{ep2e\_bond\_block.py} (bond-block divergence),
\texttt{ep2g\_two\_variable\_scaling.py} (universal $w/\delta$
scaling), \texttt{ep2h\_universality\_extension.py} (other
bonds + $n_{\max}$ + bounded-form tests),
\texttt{ep2i\_deep\_diagnostics.py} (BeH$_2$ cross-block
decomposition, H$_2$O intrinsic offset,
$\gamma(Z,n_{\max})$ surface),
\texttt{ep2j\_li\_be\_extension.py} (3e/4e atoms,
non-degeneracy qualifier),
\texttt{ep2k\_heavy\_atom\_bond\_sweep.py} (HF, H$_2$O bond
R-sweeps), \texttt{ep2L\_nmax5\_gamma.py} ($n_{\max}=5$
asymptote and Richardson extrapolation),
\texttt{ep2M\_two\_scale\_bounded.py} (rational, stretched-exp,
Hill bounded-form fits, all negative),
\texttt{ep2N\_be\_analytical.py} (Be $3\times 3$
degenerate-PT diagonalization).  Data under
\texttt{debug/data/ep2\{c,d,e,f,g,h,i\}\_*.json}; fits
regression-locked by
\texttt{tests/test\_paper27\_entropy.py::test\_paper27\_ep2c\_dimensionless\_power\_law},
\texttt{::test\_paper27\_ep2c\_multi\_block\_universality},
\texttt{::test\_paper27\_ep2d\_z\_range\_artifact},
\texttt{::test\_paper27\_ep2g\_universal\_w\_over\_delta},
\texttt{::test\_paper27\_ep2h\_nmax\_residue\_persists},
and
\texttt{::test\_paper27\_ep2i\_gamma\_asymptote}.


\medskip
We do not speculate about gravitational, holographic, or
black-hole entropy in this paper.  Whether the pair-counting
re-interpretation of the area law in Section~\ref{sec:area}
admits a continuum geometric reading is a separate question;
the operator-theoretic content stands without it.

%=================================================================
\section{Conclusion}
\label{sec:conclusion}
%=================================================================

Five results have been established on GeoVac lattices for
closed-shell two-fermion ground states.  First, the von
Neumann entropy of any single-particle reduced density matrix
derived from the \emph{non-degenerate} ground state of the
one-body Hamiltonian alone is identically zero; numerically,
$S_{\mathrm{kin}}/S_{\mathrm{full}}\sim 10^{-14}$ on He at
$n_{\max}=2,3$.  All ground-state entanglement is generated by
the two-body operator $V_{ee}$.  Second, the area-law scaling
$S_n \propto 4\log n$ asserted on one-particle grounds in
Paper~5~\cite{GeoVac_Paper5} is recovered as a two-body
pair-counting statement $A_n = g_n^{2}\propto n^{4}$, with the
factor of four as the two-body signature.  Third, the cusp
localizes as a single hot node on the $V_{ee}$ pair graph,
making the loci of energy correlation, entropy generation, and
cusp mass coincident.  Fourth, on the Bargmann--Segal harmonic
oscillator lattice, two-fermion closed-shell ground-state
entropy is identically zero for \emph{any} central two-body
potential, a rigidity statement structurally parallel to the
Fock and Bargmann--Segal theorems of Paper~24.  Fifth, on the
Coulomb lattice, block entropy obeys a universal two-variable
scaling
$S_B = A\,(\tilde{w}_B/\delta_B)^{\gamma}$ in the dimensionless
$V_{ee}$ off-diagonal mass and the first $H_1$ gap, with the
local exponent extrapolating to $\gamma_{\infty} \approx 1.96$
as $Z, n_{\max} \to \infty$ -- below the naive second-order
Rayleigh--Schr\"odinger value of $2$, by a residue attributed
to multi-shell aggregation in the basis-extension limit.

These results place entropy in the exchange-constant
taxonomy of Paper~18~\cite{GeoVac_Paper18} as a
projection-class observable: zero on the combinatorial graph,
finite only after the metric embedding of $1/r_{12}$.  The
two-body character of the area-law scaling is structural, not
incidental; the predicted contrast between Coulomb and harmonic
oscillator two-fermion lattices is sharp and falsifiable.

%=================================================================
\section*{Equation Verification}
%=================================================================

Per Section~13.4a of the project guidelines, every numerical
claim in this paper is traceable to a debug data file or to a
test in the codebase.

\begin{itemize}
  \item Table~\ref{tab:ep1}: \texttt{debug/data/ep1\_entropy\_partition.json}.
    \textbf{Verified} by
    \texttt{tests/test\_paper27\_entropy.py::\allowbreak
    test\_paper27\_table1\_ep1\_reproduction}, which re-runs the
    EP-1 pipeline (FCI of $H_1$ vs.\ $H_1+V_{ee}$ on the He
    angular momentum basis, via \texttt{geovac/casimir\_ci.py}
    and \texttt{geovac/hypergeometric\_slater.py}) and checks
    $|S_{\mathrm{kin}}|<10^{-12}$ and
    $|S_{\mathrm{kin}}|/S_{\mathrm{full}}<10^{-10}$ at
    $n_{\max}\in\{2,3\}$.
  \item Eq.~\eqref{eq:An}--\eqref{eq:logAn}: combinatorial
    identities on Paper~0 shell degeneracies $g_n=2n^{2}$.
    \textbf{Verified} symbolically by
    \texttt{tests/test\_paper27\_entropy.py::\allowbreak
    test\_paper27\_area\_law\_pair\_counts} and
    \texttt{::test\_paper27\_area\_law\_leading\_order\_agrees},
    with companion test
    \texttt{::test\_paper27\_one\_body\_counts\_differ\_from\_area\_law}
    confirming the one-particle slope is $2$ (single-shell) or
    $3$ (cumulative), distinct from the two-body slope $4$.
    Hand-tabulation in EP-3 analysis~\cite{EP3_data}.
  \item Section~\ref{sec:cusp} numerical values
    ($\lVert V_{ee}^{\mathrm{diag}\,(H_1)}\rVert_F/\lVert V_{ee}\rVert_F$,
    relative commutator, hot-node weights): produced by
    \texttt{debug/energy\_graph\_exploration.py}, data in
    \texttt{debug/data/energy\_graph\_nmax\{3,4\}.json}.
    \textbf{Verified} by
    \texttt{tests/test\_paper27\_entropy.py::\allowbreak
    test\_paper27\_energy\_graph\_structural\_invariants}
    (commutator ratio, diagonal fraction, and exact
    $V_{(1s,1s),(1s,1s)} = 5/8$ at both $n_{\max}=3$ and $4$);
    non-vanishing of the commutator in the $n_{\max}=3 \to 4$
    extension is pinned by
    \texttt{::test\_paper27\_commutator\_saturates\_not\_vanishes}
    (\texttt{--slow}).
  \item Table~\ref{tab:ep2c} and
    Eq.~\eqref{eq:pred2} (the dimensionless power law
    $S_B = 8.16\,\tilde{w}_B^{2.383}$, $R^{2}=0.998$):
    \textbf{Verified} by
    \texttt{tests/test\_paper27\_entropy.py::\allowbreak
    test\_paper27\_ep2c\_dimensionless\_power\_law},
    data in \texttt{debug/data/ep2c\_entropy\_vs\_vee\_offdiag.json}.
  \item Table~\ref{tab:ep2b} and Section~\ref{sec:pred1}
    numerical claims ($S_{\mathrm{HO}}=0$, occupation
    $(2.0,0,0,0)$, commutator at floating-point noise):
    \textbf{Verified} by
    \texttt{tests/test\_paper27\_entropy.py::\allowbreak
    test\_paper27\_ep2b\_ho\_gs\_is\_single\_determinant}
    and
    \texttt{::test\_paper27\_ep2b\_ho\_kinetic\_interaction\_commute},
    data in \texttt{debug/data/ep2b\_ho\_two\_fermion.json}.
  \item Section~\ref{sec:taxonomy} embedding-class placement:
    diagonal pair-state $V_{ii}$ rationality and spectral
    irrationality verified in EP-3 / energy-graph
    analysis~\cite{EnergyGraph}; consistent with Paper~18
    classification.
\end{itemize}

The operator-theoretic statement of Section~\ref{sec:operator}
is a known free-fermion fact~\cite{Peschel2003}; no new
verification is required beyond the numerical confirmation in
Section~\ref{sec:numerical}.

%=================================================================
% Acknowledgments
%=================================================================

\begin{acknowledgments}
This work was performed using an AI-augmented agentic workflow.
The principal investigator provided scientific direction and
quality control; implementation, computation, and documentation
drafting were performed collaboratively with large language
models (Anthropic Claude).
\end{acknowledgments}

%=================================================================
% References
%=================================================================

\begin{thebibliography}{20}

\bibitem{Peschel2003}
I.~Peschel,
``Calculation of reduced density matrices from correlation
functions,''
J.\ Phys.\ A: Math.\ Gen.\ \textbf{36}, L205 (2003).

\bibitem{LiHaldane2008}
H.~Li and F.~D.~M.~Haldane,
``Entanglement spectrum as a generalization of the entanglement
entropy,''
Phys.\ Rev.\ Lett.\ \textbf{101}, 010504 (2008).

\bibitem{GhirardiMarinatto2004}
G.~C.~Ghirardi and L.~Marinatto,
``General criterion for the entanglement of two indistinguishable
particles,''
Phys.\ Rev.\ A \textbf{70}, 012109 (2004).

\bibitem{GeoVac_Paper0}
J.~Loutey,
``Angular Momentum as Information Geometry: Isotropic Packing
and the Universal Angular Cross-Section,''
GeoVac Technical Report (2025).

\bibitem{GeoVac_Paper5}
J.~Loutey,
``The Geometric Vacuum: A Comprehensive Framework,''
GeoVac Technical Report (2025), conjectural.

\bibitem{GeoVac_Paper7}
J.~Loutey,
``The Dimensionless Vacuum: Spectral Graph Theory and Conformal
Equivalence on $S^{3}$,''
GeoVac Technical Report (2025).

\bibitem{GeoVac_Paper14}
J.~Loutey,
``Structurally Sparse Qubit Hamiltonians from Spectral Graph
Theory,''
GeoVac Technical Report (2025).

\bibitem{GeoVac_Paper18}
J.~Loutey,
``Spectral-Geometric Exchange Constants and the Transcendence
Taxonomy,''
GeoVac Technical Report (2026).

\bibitem{GeoVac_Paper23}
J.~Loutey,
``Nuclear Shell Model Qubit Hamiltonians and the Fock Projection
Rigidity Theorem,''
GeoVac Technical Report (2026).

\bibitem{GeoVac_Paper24}
J.~Loutey,
``The Bargmann--Segal Lattice: $\pi$-Free Discretization of the
3D Harmonic Oscillator,''
GeoVac Technical Report (2026).

\bibitem{GeoVac_Paper26}
J.~Loutey,
``Entanglement Structure of the Angular Momentum Eigenbasis,''
GeoVac Technical Report (2026).

\bibitem{EP1_data}
GeoVac project data file:
\texttt{debug/data/ep1\_entropy\_partition.json}.

\bibitem{EP2_data}
GeoVac project data file:
\texttt{debug/data/ep2\_coulomb\_vs\_ho\_entropy.json};
EP-2b follow-on
\texttt{debug/data/ep2b\_ho\_two\_fermion.json} and driver
\texttt{debug/ep2b\_ho\_two\_fermion.py}.

\bibitem{Moshinsky1996}
M.~Moshinsky and Yu.~F.~Smirnov,
\emph{The Harmonic Oscillator in Modern Physics}
(Harwood Academic, 1996).

\bibitem{EP3_data}
GeoVac project memo:
\texttt{debug/data/ep3\_area\_law\_analysis.md}.

\bibitem{EnergyGraph}
GeoVac v2.9.1 energy-graph characterization:
\texttt{debug/energy\_graph\_exploration.md},
\texttt{debug/data/energy\_graph\_nmax\{3,4\}.json}.

\end{thebibliography}

\end{document}
