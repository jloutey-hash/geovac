[pytest]
markers =
    slow: marks tests as slow (deselect with -m "not slow")
# Archived tests are not collected by default per CLAUDE.md §14.
# Run them explicitly via `pytest tests/_archive/` if needed.
addopts = --ignore=tests/_archive
